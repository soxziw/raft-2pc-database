syntax = "proto3";

import "appendEntriesReq.proto";

message PrintDatastoreRsp {
  int32 clusterId = 1; // Cluster that server belong to
  int32 serverId = 2; // Server's id
  repeated Entry entries = 3; // Log entries on local server
}